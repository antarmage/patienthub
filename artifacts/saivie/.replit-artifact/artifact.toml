kind = "web"
previewPath = "/"
title = "Saivie"
version = "1.0.0"
id = "artifacts/saivie"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20171

[services.development]
run = "pnpm --filter @workspace/saivie run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/saivie", "run", "build" ]
publicDir = "artifacts/saivie/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20171"
BASE_PATH = "/"
